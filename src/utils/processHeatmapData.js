/** @format */

export function processHeatmapData(parsedData, processedData) {
  const heatmapData = [];

  // Go through the data and count how many lifts per week/month/year

  return heatmapData;
}
